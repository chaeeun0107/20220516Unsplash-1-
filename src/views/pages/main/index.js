import styled from 'styled-components';

import Header from '../../shared/header';

function Main() {
  return (
    <Container>
      <Header />
    </Container>
  );
}

const Container = styled.div`

`;

export default Main;
